#!/usr/bin/env bash

# Unset all meteor related environment variables. Run:
# source unset-meteor-env.sh
# This will unset the variables below in your curent shell.
unset PORT
unset ROOT_URL
unset MONGO_URL
