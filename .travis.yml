language: node_js
node_js:
  - "0.10"
before_install:
  - "curl https://install.meteor.com | /bin/sh"
  - "meteor --version"
  - "npm install -g coffee-script"
  - "phantomjs -v"
branches:
  only:
    - master
    - devel
